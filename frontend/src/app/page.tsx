'use client'
import Link from 'next/link'
import { useWallet } from '@/contexts/WalletContext'
import { Button } from '@/components/ui/button'
import { ArrowRight, Check, Github, LockKeyhole, Search, Wallet } from 'lucide-react'

const features = [
  ['Fund real work', 'Create a clear reward for a GitHub issue without managing a spreadsheet or an intermediary.', Wallet],
  ['Track a contribution', 'Link the pull request that solves the issue and keep its state visible to everyone.', Search],
  ['Release with confidence', 'Use the Stellar escrow workflow to make reward handoff explicit and auditable.', LockKeyhole],
]
export default function Home() { const { connected, connecting, connect, freighterInstalled } = useWallet()
return <div className="space-y-24 pb-12 sm:space-y-32"><section className="grid min-h-[62vh] items-center gap-12 py-12 lg:grid-cols-[1.15fr_.85fr] lg:py-20"><div className="max-w-3xl"><p className="mb-5 text-sm font-medium text-zinc-400">CodeBounty / Stellar escrow for open source</p><h1 className="text-5xl font-semibold tracking-[-.04em] text-zinc-50 sm:text-6xl lg:text-7xl">Open-source work, funded with intent.</h1><p className="mt-6 max-w-xl text-lg leading-8 text-zinc-400">Turn a GitHub issue into a visible, accountable bounty. Fund it, follow the contribution, and settle the reward through a simple Stellar-based workflow.</p><div className="mt-9 flex flex-wrap gap-3">{connected ? <Link href="/bounties"><Button size="lg">Browse bounties <ArrowRight /></Button></Link> : freighterInstalled ? <Button size="lg" onClick={() => connect()} disabled={connecting}><Wallet /> {connecting ? 'Connecting wallet' : 'Connect wallet'}</Button> : <Link href="/bounties"><Button size="lg">Explore bounties <ArrowRight /></Button></Link>}<Link href="/bounties/create"><Button variant="outline" size="lg">Create a bounty</Button></Link></div></div><div className="rounded-2xl border border-zinc-800 bg-zinc-950 p-5 sm:p-7"><div className="flex items-center justify-between border-b border-zinc-800 pb-4 text-sm"><span className="font-medium">Bounty workflow</span><span className="text-zinc-500">3 steps</span></div><div className="space-y-6 py-7">{[['01','Add the GitHub issue'],['02','Fund the reward'],['03','Link and verify the pull request']].map(([number,label])=><div key={number} className="flex items-center gap-4"><span className="grid h-8 w-8 place-items-center rounded-full border border-zinc-700 text-xs text-zinc-400">{number}</span><span className="text-sm text-zinc-200">{label}</span></div>)}</div><div className="rounded-lg border border-zinc-800 bg-black p-4 text-sm text-zinc-400"><Github className="mr-2 inline h-4 w-4" />Built around the tools contributors already use.</div></div></section><section><div className="mb-8 max-w-xl"><p className="text-sm font-medium text-zinc-500">A focused workflow</p><h2 className="mt-2 text-3xl font-semibold tracking-tight sm:text-4xl">Less coordination. Clearer outcomes.</h2></div><div className="grid gap-4 md:grid-cols-3">{features.map(([title,body,Icon]) => { const FeatureIcon = Icon as typeof Wallet; return <article key={title as string} className="rounded-xl border border-zinc-800 bg-zinc-950 p-6"><FeatureIcon className="h-5 w-5 text-zinc-300"/><h3 className="mt-8 text-lg font-medium">{title as string}</h3><p className="mt-2 text-sm leading-6 text-zinc-400">{body as string}</p></article>})}</div></section><section className="rounded-2xl border border-zinc-800 bg-zinc-950 px-6 py-10 sm:px-10"><div className="grid gap-8 lg:grid-cols-2 lg:items-end"><div><h2 className="text-3xl font-semibold tracking-tight">Start with an issue your project already needs.</h2><p className="mt-3 max-w-xl text-zinc-400">Create a bounty with a GitHub issue URL, reward, and deadline. The details stay readable for maintainers and contributors alike.</p></div><div className="flex lg:justify-end"><Link href="/bounties/create"><Button size="lg">Create bounty <ArrowRight /></Button></Link></div></div></section></div> }
